s1 = 'Yes';
s2 = 'No';
tf = strcmp(s1,s2)

s1 = {'lag'; 'Tc'};
s2 = {'lag'; 'Tc'; 'T1'};
tf = strcmp(s1,s2) % Doesn't work?

cellfun(cellfind('lag'),s2)


X = zeros(length(vars));
for k = 1:nueml(perturbedVars)
    X(strncmp(A,B{k},3)) = k;
end
